version: "3.7"

services:
  caddy:
    image: caddy:latest
    restart: unless-stopped
    # ports:
    #   - "80:80"
    #   - "443:443"
    volumes:
      - ./container-data/Caddyfile:/etc/caddy/Caddyfile
      - ./container-data//site:/srv
      - ./container-data/caddy_data:/data
      - ./container-data/caddy_config:/config
    networks: 
      - reverseproxy

networks: 
  reverseproxy:
    external: true
