apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: homelab
resources:
  - deployment.yaml
  - service.yaml
  - ingressroute.yaml
configMapGenerator:
  - name: homer-config
    files:
      - config.yml
