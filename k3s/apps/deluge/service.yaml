# hostNetwork pod: this Service resolves to the node's IP:8112, not a pod IP.
apiVersion: v1
kind: Service
metadata:
  name: deluge
  namespace: homelab
spec:
  type: ClusterIP
  selector:
    app: deluge
  ports:
    - name: web
      port: 8112
      targetPort: 8112
